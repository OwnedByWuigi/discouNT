name: Build and Release discouNT

on:
  push:
    branches:
      - master

permissions:
  contents: write
  packages: write

jobs:
  build-x86:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            nasm \
            xorriso \
            grub-pc-bin \
            grub-common \
            gcc \
            make \
            mtools

      - name: Build x86 ISO
        run: |
          make
          
      - name: Upload x86 ISO artifact
        uses: actions/upload-artifact@v4
        with:
          name: ntos
          path: ntos.iso
          retention-days: 1

  create-release:
    needs: [build-x86]
    runs-on: ubuntu-latest
    if: always() && !failure() && !cancelled()
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download x86 ISO
        uses: actions/download-artifact@v4
        with:
          name: ntos
          path: ./artifacts

      - name: Verify ISO exists
        run: |
          if [ ! -f ./artifacts/ntos.iso ]; then
            echo "Error: ntos-x86.iso not found!"
            exit 1
          fi

      - name: Create rolling release tag
        id: tag
        run: |
          TIMESTAMP=$(date +'%Y%m%d-%H%M%S')
          TAG_NAME="rolling-${TIMESTAMP}"
          echo "tag_name=${TAG_NAME}" >> $GITHUB_OUTPUT
          
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.tag.outputs.tag_name }}
          name: "Rolling Release - ${{ steps.tag.outputs.tag_name }}"
          body: |
            ## Rolling Release
            Built from commit: ${{ github.sha }}
            
            ### Includes:
            - ntos.iso (x86 architecture)
            **AMD64 excluded for now due to the fact that my AMD64 impl broke dll imports for x86**
          files: |
            ./artifacts/ntos.iso
          draft: false
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  notify-failure:
    needs: [build-x86]
    runs-on: ubuntu-latest
    if: failure()
    steps:
      - name: Check which job failed
        run: |
          echo "One or both builds failed. No release will be created."
          echo "Check the build logs for details."